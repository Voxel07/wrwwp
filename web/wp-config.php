<?php
/**
 * This is a stub wp-config.php file.
 * The actual configuration file has been moved one directory up (outside the web root) for security.
 * WordPress requires a wp-config.php file in its root or one directory up from its root (web/wp).
 * Since the WordPress root is web/wp, it looks in web/wp-config.php.
 */
require_once dirname(__DIR__) . '/wp-config.php';
